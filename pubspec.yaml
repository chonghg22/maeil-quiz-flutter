name: maeil_quiz
description: 매일퀴즈 - 매일 새로운 퀴즈로 지식을 키우세요
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Supabase
  supabase_flutter: ^2.3.4

  # 상태 관리
  riverpod: ^2.5.1
  flutter_riverpod: ^2.5.1

  # 라우팅
  go_router: ^13.2.0

  # 로컬 저장소
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.0.0

  uuid: ^4.3.3
  cupertino_icons: ^1.0.6
  google_mobile_ads: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
